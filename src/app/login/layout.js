export const metadata = {
  title: 'Sign In',
  description:
    'Sign in to AceurExam to keep your paper scan analyses and topical packs connected to your study library.',
  alternates: { canonical: '/login' },
}

export default function LoginLayout({ children }) {
  return children
}
