export const metadata = {
  title: 'Sign In',
  description:
    'Sign in to AceurExam to keep your topical packs, examiner notes, and AI tutor sessions connected to your study library.',
  alternates: { canonical: '/login' },
}

export default function LoginLayout({ children }) {
  return children
}
